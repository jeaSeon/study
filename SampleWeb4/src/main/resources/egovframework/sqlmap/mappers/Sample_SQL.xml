<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.sqlmap.mappers.Sample_SQL">

	<!-- type이 매퍼스에 있는 알리아스명, jpa에서 엔티티의 테이블 명 같은 의미 
	property는 VO의 변수명 column은 DB의 컬럼명-->
	<resultMap type="sample" id="sampleResult">  
		<result property="id" column="ID"/>  
		<result property="title" column="TITLE"/>
		<result property="regUser" column="REG_USER"/>
		<result property="content" column="CONTENT"/>
		<result property="regDate" column="REG_DATE"/>
	</resultMap>

	<insert id="insertSample">
	insert into sample(id, title, reg_user, content, reg_date) values(
		#{id}#,
		#{title}#,
		#{regUser}#,
		#{content}#,
		now)
	</insert>
	
	<update id="updateSample">
		UPDATE SAMPLE 
		SET ID=#{id}#,
		TITLE=#{title}#,
		REG_USER=#{regUser}#,
		CONTENT=#{content}# 
		WHERE ID=#{id}#
	</update>
	
	<delete id="deleteSample">
		DELETE FROM SAMPLE
		WHERE ID=#{id}#
	</delete>
	
	<select id="selectSample" resultMap="sampleResult">
		SELECT ID, TITLE, REG_USER, CONTENT, REG_DATE 
		FROM SAMPLE 
		WHERE ID=#{id}#
	</select>
	
 

	<select id="selectSampleList" resultMap="sampleResult">
		SELECT ID, TITLE, REG_USER, CONTENT, REG_DATE 
		FROM SAMPLE
		ORDER BY ID DESC
	</select>

</mapper>